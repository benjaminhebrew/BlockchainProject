import styled from "styled-components";

export const StyledCTime = styled.div`
	color: ${({theme}) => theme.color.light}
	font-size: 1.125rem;
	letter-spacing: 0.1875rem;
	font-weight: 400;
	float: left;
`;